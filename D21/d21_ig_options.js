function(config) {
    config.defaultGridViewOptions = {
        selectionStateItem: "P01_SELECTED_IDS", // Sostituisci con il tuo elemento di pagina
        multiple: true // Permette la selezione multipla
    };
    return config;
}